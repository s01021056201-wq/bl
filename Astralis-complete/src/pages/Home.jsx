import {useEffect,useState} from "react";import AstroLayout from "../components/astro/AstroLayout";import Onboarding from "../components/astro/Onboarding";import Dashboard from "../components/astro/Dashboard";
export default function Home(){const [profile,setProfile]=useState(undefined);useEffect(()=>{try{const x=localStorage.getItem("astralis_profile");setProfile(x?JSON.parse(x):null)}catch{setProfile(null)}},[]);return <AstroLayout>{profile===undefined?<div className="text-center py-20 font-mono text-xs">Reading the local sky…</div>:profile?<Dashboard profile={profile}/>:<Onboarding onSaved={setProfile}/>}</AstroLayout>}